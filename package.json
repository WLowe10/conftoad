{
	"name": "conftoad",
	"private": true,
	"scripts": {
		"format": "prettier --write .",
		"g:publish-package": "pnpm build && pnpm publish",
		"g:build": "pnpm -r build",
		"g:clean": "pnpm -r clean"
	},
	"devDependencies": {
		"prettier": "^3.3.2"
	}
}
